[project]
name = "dobot-edge"
version = "0.1.0"
description = "Edge service for the dobot machine"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "puda-comms==0.0.10",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "python-dotenv>=1.2.1",
    "setuptools<81",
]